#include <stdio.h>
#include <stdlib.h>


int main(void) {
	long long int  a;
	scanf("%d", &a);
	printf("%lld", a+1); 
	return 0;
}


int 음수,양수 둘다 포함
long long int 사용시 양수만 표시함.

++a 변수 사용하기 전 증감을 먼저하고 
a++ 변수를 먼저 사용하고 난 후에 증감을 수행한다.
